```markdown
# Ejercicio 0.6: Nueva Nota en Aplicación de Página Única (SPA)

```mermaid
sequenceDiagram
    participant browser
    participant server

    Note right of browser: El usuario escribe la nota y hace clic en Guardar (Save)

    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa
    activate server
    Note left of server: El servidor guarda la nueva nota
    server-->>browser: 201 Created (o mensaje de éxito)
    deactivate server

    Note right of browser: El JavaScript de spa.js actualiza la lista de notas en el DOM, sin recargar la página.
